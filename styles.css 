
.simple-time-tracker-container {
  border: 1px solid var(--background-modifier-border);
  border-radius: 6px;
  padding: 12px;
  background-color: var(--background-secondary);
  margin: 8px 0;
}

.simple-time-tracker-display h4 {
  margin: 0 0 8px 0;
  color: var(--text-accent);
  font-weight: 600;
}

.time-tracker-summary {
  font-size: 0.9em;
  color: var(--text-muted);
}

.time-tracker-summary p {
  margin: 4px 0;
}

.simple-time-tracker-error {
  color: var(--text-error);
  font-style: italic;
  padding: 8px;
  background-color: var(--background-modifier-error);
  border-radius: 4px;
}

.invoice-button-container {
  display: flex;
  justify-content: flex-end;
  gap: 8px;
}

.invoice-button-container button {
  transition: all 0.2s ease;
}

.invoice-button-container button:hover {
  transform: translateY(-1px);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
}
